{
    "version": 2,
    "buildCommand": "cp package.vercel.json package.json && cp next.config.vercel.js next.config.js && node pre-build.js && npm install && npm run build",
    "outputDirectory": "dist",
    "installCommand": "echo 'Using buildCommand for installation'"
}